backend:
  db:
    host: db
    name: appdb
    username: appuser
    password: appsecret

mysql:
  fullnameOverride: db
  auth:
    database: appdb
    username: appuser
    password: appsecret
    rootPassword: rootpassword
  primary:
    persistence:
      enabled: true
      size: 1Gi